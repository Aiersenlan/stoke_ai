name: Keep Render Alive (Extra 3)
on:
  schedule:
    - cron: '1,6,11,16,21,26,31,36,41,46,51,56 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: 喚醒脈衝
        run: curl -s -L "https://institutional-tracker.onrender.com/health" > /dev/null
