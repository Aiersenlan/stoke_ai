name: Keep Render Alive (Extra 4)
on:
  schedule:
    - cron: '3,8,13,18,23,28,33,38,43,48,53,58 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: 喚醒脈衝
        run: curl -s -L "https://institutional-tracker.onrender.com/health" > /dev/null
