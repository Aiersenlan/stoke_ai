name: Keep Render Alive (Extra 5)
on:
  schedule:
    - cron: '4,9,14,19,24,29,34,39,44,49,54,59 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: 喚醒脈衝
        run: curl -s -L "https://institutional-tracker.onrender.com/health" > /dev/null
